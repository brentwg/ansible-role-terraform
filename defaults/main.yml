---
terraform_required_packages:
  - "unzip"

terraform_version: "1.2.0"
terraform_arch: "amd64"
terraform_archive_dir: "/usr/local/terraform/{{ terraform_version }}"
terraform_archive_path: "{{ terraform_archive_dir }}/terraform"
terraform_link_path: /usr/local/bin/terraform
